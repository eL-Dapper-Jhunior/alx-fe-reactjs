module.exports = {
    content: ["./src/**/*.{js,jsx,ts,tsx}", "./public/index.html"], // Paths to scan for used styles
    theme: { extend: {} }, // Extend Tailwind's default styles if needed
    plugins: [], // Add extra Tailwind plugins here if needed
  };
  